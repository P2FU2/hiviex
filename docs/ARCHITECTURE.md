# Arquitetura da Plataforma HIVIEX SaaS

## 🏛️ Arquitetura de Alto Nível

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend (Next.js)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Pages   │  │ Components│  │  Hooks   │  │  Context │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  Next.js API Routes                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Auth    │  │  Agents  │  │   Chat   │  │ Billing  │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────────┘
         │            │            │            │
         ▼            ▼            ▼            ▼
┌─────────────────────────────────────────────────────────────┐
│                      Services Layer                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Agent   │  │   Chat   │  │ Workflow │  │  Billing │  │
│  │ Service  │  │  Service │  │  Service │  │  Service │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────────┘
         │            │            │            │
         ▼            ▼            ▼            ▼
┌─────────────────────────────────────────────────────────────┐
│                    Infrastructure                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │PostgreSQL│  │   Redis   │  │    S3    │  │  Socket  │  │
│  │(pgvector)│  │  (BullMQ) │  │  (R2)    │  │    IO    │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🔑 Componentes Principais

### 1. Frontend (Next.js App Router)
- **Server Components**: Para dados e SEO
- **Client Components**: Para interatividade
- **Route Handlers**: API endpoints
- **Server Actions**: Mutations server-side

### 2. Autenticação (NextAuth.js)
- Multi-tenant por workspace
- Sessions gerenciadas
- RBAC integrado

### 3. Banco de Dados (PostgreSQL + Prisma)
- **Tenant**: Isolamento multi-tenant
- **User**: Usuários com roles
- **Agent**: Configurações de agentes
- **Message**: Histórico de chat
- **Workflow**: Automações
- **Subscription**: Planos e billing

### 4. Filas (BullMQ + Redis)
- Jobs assíncronos
- Retry logic
- Priorização
- Workers escaláveis

### 5. LLM Providers
- Abstração plugável
- Múltiplos providers
- Fallback automático
- Rate limiting

### 6. Armazenamento (S3/R2)
- Mídias de agentes
- Arquivos de workflows
- CDN integration

### 7. WebSockets (Socket.IO)
- Chat em tempo real
- Notificações
- Updates de status

### 8. Billing (Stripe)
- Planos e preços
- Medição de uso
- Webhooks
- Invoices

## 🔄 Fluxo de Dados

### Criação de Agente
```
User → API Route → Agent Service → Prisma → PostgreSQL
                                          ↓
                                    S3 (Upload mídias)
```

### Chat com Agente
```
User → WebSocket → Chat Service → LLM Provider → Response
                                      ↓
                                Prisma (Save message)
```

### Workflow Execution
```
Trigger → Queue (BullMQ) → Worker → n8n/Service → Action
```

## 🔐 Segurança

- Multi-tenant isolation
- RBAC per resource
- Rate limiting
- Input validation (Zod)
- CORS configuration
- Secrets management

## 📈 Escalabilidade

- Horizontal scaling (Next.js stateless)
- Database connection pooling
- Redis para cache e filas
- CDN para assets
- Worker processes separados

